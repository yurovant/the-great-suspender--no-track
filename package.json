{
  "name": "the-great-suspender-notrack",
  "version": "1.0.0",
  "description": "An extension for suspending browser tabs to free up memory. Without analytics tracking.",
  "scripts": {
    "build": "grunt",
    "generate-key": "openssl genrsa -out key.pem",
    "prettify": "prettier --write '**/*.{js,css,json,md}'"
  },
  "devDependencies": {
    "grunt": "1.6.1",
    "grunt-cli": "1.4.3",
    "grunt-contrib-clean": "2.0.1",
    "grunt-contrib-copy": "1.0.0",
    "grunt-crx": "2.0.0",
    "grunt-string-replace": "1.3.3",
    "prettier": "^3.0.3",
    "time-grunt": "2.0.0"
  },
  "dependencies": {
    "db.js": "^0.15.0"
  }
}
